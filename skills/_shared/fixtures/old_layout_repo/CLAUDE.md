# Project Instructions

This project uses TDD skills:

- Run `/tdd-setup` first to establish vision.
- Designs are tracked under `docs/tdd-designs/{name}/`.
- Use `/tdd-design` and `/tdd-phase-implement` to drive work.

See `docs/vision.md` for the why.
