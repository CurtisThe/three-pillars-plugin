# Plugin Instructions

Use `/tdd-design` for full builds, `/tdd-spike` for experiments. Designs live
under `docs/tdd-designs/{name}/` and archived ones under
`docs/completed-tdd-designs/`. See `docs/architecture.md` for the system shape.
