{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "urn:tp-run-full-design:generator-return:v1",
  "title": "tp-run-full-design generator tier return",
  "description": "Return envelope for generator-class slots (design, detail, plan, design-learn). Extends the base tier-return via allOf and adds the artifact paths the tier wrote to disk.",
  "allOf": [
    {"$ref": "urn:tp-run-full-design:tier-return:v1"}
  ],
  "type": "object",
  "required": [
    "artifact_paths"
  ],
  "additionalProperties": true,
  "properties": {
    "schema": {
      "const": "tp-run-full-design/generator-return/v1"
    },
    "artifact_paths": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string", "minLength": 1}
    }
  }
}
